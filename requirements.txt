discord.py
pexpect
psutil
python-dotenv
pytest
pytest-asyncio

# LLM Providers (install the one you need)
google-genai  # For Gemini
# openai>=1.0.0  # For OpenAI (optional)
# anthropic>=0.18.0  # For Anthropic (optional)
ollama>=0.4.0  # For Ollama (local models)

# Redis (optional - enables persistence features)
redis

# Browser automation for Canopus
playwright

# HTTP client for ngrok API
httpx

# Polaris API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
PyJWT>=2.8.0
pwdlib[bcrypt]>=0.2.0
pywebpush>=2.0.0
py-vapid>=1.9.0
websockets>=12.0
